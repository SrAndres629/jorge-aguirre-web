# AUDIT PLAN - GENERATED BY GEMINI ARCHITECT

## CRITICAL SECURITY
- [ ] TASK: `core/app/config.py` - The `ADMIN_KEY` has a hardcoded default. Update it to prioritize `os.getenv("ADMIN_KEY")` and only use the default if strictly necessary (or warn).
- [ ] TASK: `core/app/config.py` - Add `jorge-aguirre-web.onrender.com` to `ALLOWED_HOSTS` or similar if applicable (FastAPI handles this in main.py, check there).

## CODE QUALITY
- [ ] TASK: `core/main.py` - Add `https://jorge-aguirre-web.onrender.com` to the `allow_origins` list in `CORSMiddleware`.
- [ ] TASK: `core/main.py` - Remove the commented out lines `# app.add_middleware(SlowAPIMiddleware)` if they are not being used, to clean up the code.

## VERIFICATION
- [ ] TASK: `core/app/database.py` - Verify that `get_cursor` function correctly handles the logic split between Postgres and SQLite and does not cause generator errors.
